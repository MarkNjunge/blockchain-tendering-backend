{
  "name": "blockchain-tendering",
  "version": "1.0.0",
  "description": "Blockchain-based tendering system.",
  "author": "Mark Njung'e",
  "license": "MIT",
  "private": false,
  "scripts": {
    "admin:clean-docker": "yarn --cwd admin clean-docker",
    "admin:create-participant": "yarn --cwd admin create-participant",
    "admin:inspect-blocks": "yarn --cwd admin inspect-blocks",
    "api:start:dev": "yarn --cwd api start:dev",
    "composer:build": "yarn --cwd composer build",
    "composer:deploy": "yarn --cwd composer deploy",
    "composer:upgrade:patch": "yarn --cwd composer upgrade:patch",
    "composer:start:rest": "yarn --cwd composer start:rest",
    "composer:start": "yarn --cwd composer start"
  }
}
